# Leave both empty to run in demo mode (mocked responses).
# Set both to wire the UI to your live backend.

# Base URL of the backend API (no trailing slash)
VITE_API_URL=

# API key issued from the platform settings
VITE_API_KEY=
